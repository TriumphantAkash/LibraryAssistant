<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="triumphantakash.github.io.librarymanager" >
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:logo="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".Activities.book_list.BookListActivity"
            android:launchMode="singleTop"
            android:icon="@drawable/ic_launcher"
            android:label="ByteMark Library Books">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.book_details.BookDetailsActivity"
            android:icon="@drawable/ic_launcher"
            android:label="Book Details">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.book_list.BookListActivity" />
        </activity>
        <activity android:name=".Activities.add_book.AddBookActivity"
            android:icon="@drawable/ic_launcher"
            android:label="Add Book">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.book_list.BookListActivity" />
        </activity>
    </application>

</manifest>
